%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% Contents : The reconciliation chapter
% Translation of the French & English versions and extracts from the German version 2.0.0 - thanks to Martin Stromberger
% $Id : 10-grisbi-manuel-de.tex, v 3.0 2026/08 Dominique Brochard
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%


\chapter{Planer\label{plannedtransactions}}
%\chapter{Scheduler\label{plannedtransactions}}
%\chapter{Échéancier\label{plannedtransactions}}

Mit dem Planer können Sie wiederkehrende Buchungen mit bestimmten Daten oder Zeitintervallen planen. Sobald eine Buchung in den Planer eingegeben wurde, kopiert Grisbi die geplante Buchung automatisch in die Buchungsliste, sobald ihr \indexword{Fälligkeitsdatum}\index{Fälligkeitsdatum} erreicht ist.
%The scheduler allows you to plan recurring transactions with specific dates or time intervals. Once a transaction is entered into the scheduler, Grisbi automatically copies the scheduled transaction to the transaction list as soon as its \indexword{due date}\index{due!date} is reached.
%L'échéancier permet de planifier des opérations qui reviennent régulièrement avec des dates ou des intervalles de temps déterminés. Une fois qu'une opération est enregistrée dans l'échéancier, Grisbi recopie automatiquement l'opération planifiée dans la liste des opérations dès que sa \indexword{date d'échéance}\index{date!échéance} est atteinte.


Darüber hinaus zeigt Grisbi auf der Startseite Benachrichtigungen an, wenn die Fälligkeitstermine für diese Buchungen anstehen (siehe Abschnitt \vref{home-details-homepage}, \menus{Startseite}).
%Furthermore, Grisbi displays alerts on the home page when the due dates for these transactions are due (see the section \vref{home-details-homepage}, \menus{Homepage}).
%De plus, Grisbi affiche dans la page d'accueil des alertes au déclenchement des échéances de ces opérations (voir la section \vref{home-details-homepage}, \menus{Affichage de la page d'accueil}).

% espace pour changement de thème

\vspacepdf{2mm}
Um auf geplante Buchungen zuzugreifen, klicken Sie im Navigationsbereich auf \menus{Planer} oder wählen Sie in der Informationsleiste \menus{Geplante Buchungen} aus (siehe Abschnitt \vref{home}, \menus{Startseite}).
%To access scheduled transactions, click on \menus{Scheduler} in the navigation panel, or select \menus{Scheduled transactions} from the information bar (see the chapter \vref{home}, \menus{Home}).
%Pour accéder aux opérations planifiées, cliquez sur \menus{Échéancier} dans le panneau de navigation, ou sélectionnez \menus{Opérations planifiées} avec la barre d'information (voir le chapitre \vref{home}, \menus{Accueil}).

\begin{figure}[htbp]
	\begin{center}
		\includegraphics[width=0.95\textwidth]{image/screenshot/planned_transactions_display}
	\end{center}
	\caption{Geplante Transaktionen anzeigen}
	%\caption{Viewing scheduled transactions}
	%\caption{Affichage des opérations planifiées}
	\label{planned-transactions-display-img}
\end{figure}

Am unteren Rand des Navigationsbereichs wird ein Kalender angezeigt, und der Detailbereich enthält nun drei Elemente\refimage{planned-transactions-display-img}:
%A calendar appears at the bottom of the navigation panel, and the details panel then contains three elements\refimage{planned-transactions-display-img}:
%Un calendrier s'affiche en bas du panneau de navigation, et le pavé des détails possède alors trois éléments\refimage{planned-transactions-display-img}:
\begin{itemize}
	\item die Werkzeugliste;
	%\item the toolbar;
	%\item la barre d'outils;
	\item die Liste der geplanten Buchungen;
	%\item the list of scheduled transactions;
	%\item la liste des opérations planifiées;
	\item the Buchungsformular
	%\item the transaction/scheduled form.
	%\item le formulaire de saisie des opérations planifiées.
\end{itemize}

% espace pour changement de thème
\vspacepdf{2mm}
Sie können die Einstellungen für Benachrichtigungen zu geplanten Buchungen im Menü \menus{Bearbeiten – Einstellungen – Allgemein – Allgemeines} konfigurieren (siehe Abschnitt \vref{setup-general-planned}, \menus{Planer}).
%You can configure the schedule alert settings in the menu \menus{Edit - Preferences - Generalities - Various settings} (see the section \vref{setup-general-planned}, \menus{Scheduler}).
%Vous pouvez configurer les paramètres des alertes de l'échéancier dans le menu \menus{Éditer - Préférences - Généralités - Paramètres divers} (voir la section \vref{setup-general-planned}, \menus{Échéancier}).


\section{Werkzeugliste\label{plannedtransactions-functions}}
%\section{Toolbar\label{plannedtransactions-functions}}
%\section{Barre d'outils\label{plannedtransactions-functions}}

Die Werkzeugliste für geplante Buchungen bietet folgende Funktionen:
%The planned transactions toolbar offers the following functions:
%La barre d'outils des opérations planifiées présente les fonctions suivantes:
\begin{itemize}
	\item \menus{Geplante Buchung erstellen}: Öffnet das leere Buchungsformular;
	%\item \menus{New scheduled}: opens the blank Transaction/Scheduled form;
	%\item \menus{Nouvelle Échéance}: ouvre le formulaire de saisie vierge;
	\item \menus{Löschen}: Löscht den ausgewählten geplanten Buchung;
	%\item \menus{Delete}: deletes the selected scheduled transaction;
	%\item \menus{Supprimer}: supprime l'opération planifiée sélectionnée;
	\item \menus{Bearbeiten}: Öffnet das Buchungsformular für den ausgewählten Buchung und ermöglicht dessen Bearbeitung;
	%\item \menus{Edit}: opens the Transaction/Scheduled form for the selected transaction and allows you to edit it;
	%\item \menus{Éditer}: ouvre le formulaire de saisie pour l'opération sélectionnée et permet de la modifier;
	\item \menus{Notizen} oder \menus{Frequenz/Art}: Blendet die Spalte \menus{Notizen} anstelle der Spalten \menus{Frequenz} und \menus{Art} ein oder umgekehrt;
	%\item \menus{Notes} or \menus{Frequency/Mode}: displays the \menus{Notes} column instead of the \menus{Frequency} and \menus{Mode} columns, or vice versa;
	%\item \menus{Remarques} ou \menus{Périodicité/Mode}: affiche la colonne \menus{Remarques} à la place des colonnes \menus{Périodicité} et \menus{Mode}, ou inversement;
	\item \menus{Ausführen}: Führt den ausgewählten geplanten Buchung aus, d. h. Übernimmt ihn in die Buchungsliste des betreffenden Kontos (Abschnitt \vref{plannedtransactions-execution});
	%\item \menus{Execute}: executes the selected scheduled transaction, i.e. copies it into the list of transactions for the relevant account (section \vref{plannedtransactions-execution});
	%\item \menus{Exécuter}: exécute l'opération planifiée sélectionnée, c'est-à-dire la recopie dans la liste des opérations du compte concerné (section \vref{plannedtransactions-execution});
	\item \menus{Ansicht}: Über ein Dropdown-Menü können Sie die Anzeige der geplanten Buchung auswählen: alle, nach verschiedenen Zeiträumen oder nach einer benutzerdefinierten Einstellung (Abschnitt \vref{plannedtransactions-view}).
	%\item \menus{View}: a drop-down list allows you to choose how scheduled transactions are displayed: all, according to various frequencies, or according to a custom view (section \vref{plannedtransactions-view}).
	%\item \menus{Affichage}: une liste déroulante permet de choisir l'affichage des opérations planifiées: toutes, selon diverses périodicités, ou selon un réglage personnalisé (section \vref{plannedtransactions-view}).
\end{itemize}


\section{Liste der geplanten Buchungen\label{plannedtransactions-list}}
%\section{List of scheduled transactions\label{plannedtransactions-list}}
%\section{Liste des opérations planifiées\label{plannedtransactions-list}}


\subsection{Beschreibung\label{plannedtransactions-list-description}}
%\subsection{Description\label{plannedtransactions-list-description}}
%\subsection{Description\label{plannedtransactions-list-description}}

Die Liste der geplanten Buchungen wird im Bereich details angezeigt\refimage{planned-transactions-list-img}.
%The list of scheduled transactions is displayed in the details panel\refimage{planned-transactions-list-img}.
%La liste des opérations planifiées s'affiche dans le panneau des détails\refimage{planned-transactions-list-img}.

% image centrée
\begin{figure}[htbp]
	\begin{center}
		\includegraphics[width=0.95\textwidth]{image/screenshot/planned_transactions_list}
	\end{center}
	\caption{Liste der geplanten Buchungen}
	%\caption{List of scheduled transactions}
	%\caption{Liste des opérations planifiées}
	\label{planned-transactions-list-img}
\end{figure}

Oben wird die Spaltenüberschriftenleiste angezeigt. Sie können \indexword{eine Spalte verbreitern oder verengen}\index{column!width}, indem Sie auf den Trennbalken zwischen zwei Spalten klicken und ihn ziehen. Um die \indexword{Spaltenbreiten}\index{Spalte!Breiten} auf ihre Standardwerte zurückzusetzen, wählen Sie den Menüpunkt \menus{Ansicht - Spaltenbreite zurücksetzen} aus der Menüleiste.
%The column heading bar appears at the top. You can \indexword{widen or narrow a column}\index{column!width} by clicking on the separator between two columns and dragging it. To restore the \indexword{column widths}\index{column!width} to their default values, select the \menus{View – Reset the column width} menu from the menu bar.
%Elle affiche en haut la barre des libellés des colonnes. Vous pouvez \indexword{élargir ou rétrécir une colonne}\index{colonne!largeur} en cliquant sur le séparateur entre deux colonnes et en le déplaçant. Pour rétablir la \indexword{largeur des colonnes}\index{colonne!largeur} à leur valeur par défaut, sélectionnez le menu \menus{Affichage - Réinitialiser la largeur des colonnes} dans la barre de menu.

Sie können die Liste der geplanten Buchungen mit dem Mausrad oder mit der Maus und dem vertikalen Bildlaufbalken nach oben oder unten verschieben. Eine eventuelle Verschiebung nach links oder rechts erfolgt mit der Maus und dem horizontalen Bildlaufbalken.
%You can scroll the list of scheduled transactions up or down using the mouse wheel, or by using the mouse and the vertical scroll bar. To scroll left or right, use the mouse and the horizontal scroll bar.
%Vous pouvez déplacer la liste des opérations planifiées vers le haut ou vers le bas avec la molette de la souris, ou bien avec la souris et l'ascenseur vertical. Le déplacement éventuel vers la gauche ou la droite se fait avec la souris et l'ascenseur horizontal.

% espace pour changement de thème
\vspacepdf{2mm}
Jeder Buchung wird in einer einzigen Zeile und in maximal sechs Spalten angezeigt, was maximal sechs Informationsfelder für jeden geplanten Buchung ergibt. Die Anzeigefelder sowie die \indexword{Spaltenbezeichnungen}\index{Spalte!Bezeichnung} lauten wie folgt:
%Each transaction is displayed on a single line and across a maximum of six columns, providing a maximum of six information fields for each scheduled transaction. The display fields, along with the \indexword{column headings}\index{column!heading}, are as follows:
%Chaque opération est affichée sur une seule ligne et au maximum six colonnes, ce qui fait au maximum six champs d'information pour chaque opération planifiée. Les champs d'affichage, ainsi que les \indexword{libellés des colonnes}\index{colonne!libellé}, sont les suivants:
\begin{itemize}
	\item \menus{Datum}: das Datum der Buchung;
	%\item \menus{Date}: the date of the transaction;
	%\item \menus{Date}: la date de l'opération;
	\item \menus{Konto}: das betreffende Konto;
	%\item \menus{Account}: the account in question;
	%\item \menus{Compte}: le compte concerné;
	\item \menus{Empfänger}: 
	%\item \menus{Payee}: 
	%\item \menus{Tiers};
	\item \menus{Frequenz}: die Frequenz der Transaktion;
	%\item \menus{Frequency}: the frequency of the transaction;
	%\item \menus{Périodicité}: la périodicité de l'opération;
	\item \menus{Art}: die Ausführungsart, automatisch oder manuell;
	%\item \menus{Mode}: the method of execution, automatic or manual;
	%\item \menus{Mode}: le mode d'exécution, automatique ou manuel;
	\item \menus{Betrag}: den Buchungsbetrag; ein \indexword{negativer Betrag}\index{Betrag!negativ} wird in \textcolor{red}{Rot} angezeigt.
	%\item \menus{Amount}: the transaction amount; a \indexword{negative amount}\index{amount!negative} is displayed in \textcolor{red}{red}.
	%\item \menus{Montant}: le montant de l'opération; un \indexword{montant négatif}\index{montant!négatif} est affiché en \textcolor{red}{rouge}.
	% saut de ligne pour indentation correcte de la note dans la liste
	
	\Note{}: Wenn das Feld \menus{Notizen} angefordert wurde, wird es anstelle der Felder \menus{Frequenz} und \menus{Art} angezeigt, sodass nur fünf Spalten und fünf Felder angezeigt werden (siehe Abschnitt \vref{plannedtransactions-comments}, \menus{Kommentare zu geplanten Buchungen}).
	%\Note{}: if the \menus{Notes} field has been requested, it is displayed in place of the \menus{Frequency} and \menus{Method} fields, resulting in only five columns and five fields being displayed (see section \vref{plannedtransactions-comments}, \menus{Comments on planned transactions}).
	%\Note{}: si l'affichage du champ \menus{Remarques} a été demandé, il s'affiche à la place des champs \menus{Périodicité} et \menus{Mode}, ce qui ne fait plus que cinq colonnes et cinq champs affichés (voir la section \vref{plannedtransactions-comments}, \menus{Remarques des opérations planifiées}).
\end{itemize}

% espace pour changement de thème
\vspacepdf{2mm}
Direkt unterhalb der letzten Buchungszeile erscheint eine zusätzliche Leerzeile, die zum Anlegen einer neuen geplanten Buchung dient (siehe Abschnitt \vref{plannedtransactions-new}, \menus{Neue geplante Buchung}).
%An additional blank line appears just below the last transaction line and is used to create a new scheduled transaction (see section \vref{plannedtransactions-new}, \menus{New Scheduled Transaction}).
%Une ligne supplémentaire vide s'affiche juste en-dessous de la dernière ligne d'opération, et sert à créer une nouvelle opération planifiée (voir la section \vref{plannedtransactions-new}, \menus{Nouvelle opération planifiée}).

% espace pour changement de thème
\vspacepdf{2mm}
Um die Lesbarkeit der Anzeige zu verbessern, wechselt Grisbi bei jeder Zeile zwischen den Farben \colorbox[RGB]{215,215,255}{violetter Hintergrund} und Weiß.
%To improve the readability of the display, Grisbi alternates between a \colorbox[RGB]{215,215,255}{purple background} and white for each line.
%Pour des raisons de lisibilité de l'affichage, Grisbi présente une alternance de couleurs de \colorbox[RGB]{215,215,255}{fond violet} et blanc à chaque ligne.

% espace pour changement de thème
\vspacepdf{2mm}
Durch Klicken auf das Werkzeug \menus{Ansicht}\label{plannedtransactions-view} in der Symbolleiste können Sie über eine Dropdown-Liste\refimage{planned-transactions-view-select-img} zwischen verschiedenen Anzeigemodi für anstehende Buchungen \textit{ab dem heutigen Datum} wählen:
%By clicking on the \menus{View}\label{plannedtransactions-view} tool in the toolbar, a drop-down list\refimage{planned-transactions-view-select-img} allows you to choose from several display modes for upcoming transactions \textit{from today’s date}:
%En cliquant sur l'outil \menus{Affichage}\label{plannedtransactions-view} dans la barre d'outils, une liste déroulante\refimage{planned-transactions-view-select-img} vous permet de choisir plusieurs modes d'affichage des prochaines occurrences \textit{à compter de la date du jour}:
% espace avant image 5mm

\vspacepdf{5mm}
\noindent
\begin{minipage}{.65\linewidth}
	\begin{itemize}[rightmargin=.6cm]
		\item \menus{Alle Buchungen}: der nächste Termin jedes Buchung;
		%\item \menus{Unique view}: the next occurrence of each transaction;
		%\item \menus{Vue unique}: la prochaine occurrence de chaque opération;
		\item \menus{Ansicht nach Wochen}: fällig in 7 Tagen;
		%\item \menus{Week view}: due in 7 days;
		%\item \menus{Vue hebdomadaire}: à échéance de 7 jours;
		\item \menus{Ansicht nach Monaten}: innerhalb von 30 Tagen;
		%\item \menus{Month view}: within 30 days;
		%\item \menus{Vue mensuelle}: à échéance de 30 jours;
		\item \menus{Ansicht nach 2 Monaten}: innerhalb von 60 Tagen;
		%\item \menus{Two months view}: within 60 days;
		%\item \menus{Vue bimestrielle}: à échéance de 60 jours;
		\item \menus{Ansicht Vierteljährlich}: innerhalb von 90 Tagen;
		%\item \menus{Quarter view}: within 90 days;
		%\item \menus{Vue trimestrielle}: à échéance de 90 jours;
		\item \menus{Ansicht Jährlich}: innerhalb eines Jahrs;
		%\item \menus{Year view}: within one year;
		%\item \menus{Vue annuelle}: à échéance d'un an;
		\item \menus{Ansicht benutzerdefiniert}: mit einer Laufzeit von einer beliebigen Anzahl von Tagen, Wochen, Monaten oder Jahren.
		%\item \menus{Custom view}: within any number of days, weeks, months or years.
		%\item \menus{Vue personnalisée}: à échéance d'un nombre quelconque de jours, semaines, mois ou années.
		% saut de ligne pour indentation correcte de la note dans la liste
		
		\Note{}: Die aktuell angezeigte Ansicht wird durch ein Häkchen gekennzeichnet, und in Klammern wird die Anzahl der Buchungen in der ausgewählten Ansicht angezeigt.
		%\Note{}: the current view is indicated by a tick, and the number of transactions in the selected view is shown in brackets.
		%\Note{}: l'affichage courant y est indiqué par une coche et, entre parenthèses, vous avez le nombre d'opérations dans la vue sélectionnée.
	\end{itemize}
\end{minipage}
\hspace{15pt}
\begin{minipage}{.35\linewidth}
	\vspace{-5pt}
	\centering
	\includegraphics[width=.8\textwidth]{image/screenshot/planned_transactions_view_select}
	%\vspace{-5pt}
	\captionsetup{%						% for a change of options of the caption package
		format=plain,						% to avoid error message with labelsep option below, default=hang
		type=figure,%
		name=Fig.,%							% rename label caption, default="Figure" (or "Table")
		justification=centering,			% centring of text and caption label 
		labelsep=newline}					% define the separator between the label and the caption text
	\caption{Auswahl der Ansicht}
	%\caption{View selection}
	%\caption{Choix d'affichage}
	\vspace{-10pt}
	\label{planned-transactions-view-select-img}
\end{minipage}	

\vspacepdf{5mm}
Wenn Sie \menus{Ansicht benutzerdefiniert} wählen, öffnet sich ein neues Fenster mit einem Eingabefeld zur Auswahl der Anzahl der Zeiträume und einer Dropdown-Liste für die Art des gewünschten Zeitraums.
%If you select \menus{Custom view}, a new window will appear showing an incremental field to select the number of periods and a drop-down list for the type of period required\refimage{planned-transactions-view-custom-img}.
%Si vous choisissez \menus{Vue personnalisée}, une nouvelle fenêtre affiche un champ incrémental pour choisir le nombre de périodes et une liste déroulante pour le type de la période désirée\refimage{planned-transactions-view-custom-img}.

% image centrée 
\begin{figure}[htbp]
	\begin{center}
		\includegraphics[width=0.5\textwidth]{image/screenshot/planned_transactions_view_custom}
	\end{center}
	\vspace{-10pt}
	\caption{Auswahl der Darstellung des benutzerdefinierten Zeitraums}
	%\caption{Selecting the custom period view}
	%\caption{Choix de l'affichage de la période personnalisée}
	\vspace{-10pt}
	\label{planned-transactions-view-custom-img}
\end{figure}
% image centrée 

\subsection{Sortierungen\textcolor{red}{--ZU KORRIGIEREN--}\label{plannedtransactions-list-sorts}}
%\subsection{Sorts\label{plannedtransactions-list-sorts}}
%\subsection{Tris\label{plannedtransactions-list-sorts}}
%TODO : subsection to update - bug 2391
To change the order in which transactions are displayed, you can \indexword{sort the transactions}\index{sort!transactions} by one of the information fields; proceed as follows:
%Pour changer l'ordre d'affichage des opérations, vous pouvez faire un \indexword{tri sur les opérations}\index{tri!opérations} sur un des champs d'information; procédez comme suit:

\begin{enumerate}
	%\item \cmds{right-click} on the column header containing the field selected as the \gls{sorting} criterion;
	%\item cliquez-droit sur le libellé de la colonne qui contient le champ d'information choisi comme critère de \gls{tri};
	\item a context menu displays a list of the fields in that column; select the field you wish to use as the sort criterion;
	%\item un menu contextuel affiche la liste des champs de cette colonne; sélectionnez le champ choisi comme critère de tri;
	\item all transactions are displayed, sorted according to the selected criterion;
	%\item toutes les opérations s'affichent, triées selon le critère choisi;
	\item a small black triangle appears to the right or left of the column heading; it points downwards if the sort is ascending, and upwards if it is descending;
	%\item un petit triangle noir apparaît à droite ou à gauche du libellé de la colonne; il pointe vers le bas si le tri est ascendant, vers le haut si le tri est descendant;
	
	% saut de ligne pour indentation correcte de la note dans la liste
	\Note{}: depending on the theme of your desktop environment or the window manager you are using, these triangles can be replaced with other characters such as +, -, >, <, etc.
	%\Note{}: ces triangles peuvent être remplacés, en fonction du thème de l'environnement de bureau ou du gestionnaire de fenêtres que vous utilisez, par d'autres caractères tels que +, -, >, <, etc.
	\item click on this triangle to change the sort order.
	%\item cliquez sur ce triangle pour changer l'ordre du tri.
\end{enumerate}

\Note{}: sorting the display in this way does not affect the balance for each line, which is calculated after each transaction; depending on the sort order and display options you have chosen, the balance for each line may not appear in chronological order.
%\Note{}: un tri sur l'affichage réalisé ainsi n'affecte en aucun cas le solde à chaque ligne, qui est calculé après chaque opération; suivant le tri et les options d'affichage que vous avez choisi, le solde à chaque ligne peut présenter une évolution non chronologique.

%espace pour changement de thème
\vspacepdf{3mm}
You can also configure the general sort order in the \menus{Edit - Preferences} menu (see the section \vref{setup-operations-list-modes}, \menus{Transaction list behavior}).
%Vous pouvez aussi configurer l'ordre général des tris dans le menu \menus{Éditer - Préférences} (voir la section \vref{setup-operations-list-modes}, \menus{Comportement de la liste des opérations}).

\vspacepdf{3mm}
%\Note{}: the choice of the \gls{primary sorting} criterion (transaction date or value date) that may have been selected there will necessarily affect the balance displayed on each transaction line, as Grisbi calculates and displays the balance on each line.
%\Note{}: le choix du critère de \gls{tri primaire} (date d'opération ou date de valeur) qui peut y avoir été fait modifie nécessairement le solde affiché à chaque ligne d'opération, puisque Grisbi calcule et affiche le solde à chaque ligne.




%%%   To change the order in which scheduled transactions are displayed, you can \indexword{sort by transactions}\index{sort!transactions} on one of the information fields; proceed as follows:
%Pour changer l'ordre d'affichage des opérations planifiées, vous pouvez faire un \indexword{tri sur les opérations}\index{tri!opérations} sur un des champs d'information; procédez comme suit:

\begin{enumerate}
	%\item click on the column heading containing the information field selected as the \gls{sorting} criterion; the only possible sort criteria are \menus{Date}, \menus{Account} and \menus{Payee};
	%\item cliquez sur le libellé de la colonne qui contient le champ d'information choisi comme critère de \gls{tri}; les seuls critères de tri possibles sont la \menus{Date}, le \menus{Compte} et le \menus{Tiers};
	\item all transactions are displayed, sorted according to the selected criterion;
	%\item toutes les opérations s'affichent, triées selon le critère choisi;
	\item a small black triangle appears to the right of the column heading; it points downwards if the sort is ascending, and upwards if it is descending;
	%\item un petit triangle noir apparaît à droite du libellé de la colonne; il pointe vers le bas si le tri est ascendant, vers le haut si le tri est descendant;
	
	\Note{}: 
	%\Note{}: ces triangles peuvent être remplacés, en fonction du thème de l'environnement de bureau ou du gestionnaire de fenêtres que vous utilisez, par d'autres caractères tels que +, -, >, <, etc.
	
	\item 
	%\item cliquez sur ce triangle pour changer l'ordre du tri.
\end{enumerate}


\section{Geplanter Buchungsformular\label{plannedtransactions-form}}
%\section{Form for entering scheduled transactions\label{plannedtransactions-form}}
%\section{Formulaire de saisie des opérations planifiées\label{plannedtransactions-form}}

Das Geplanter Buchungsformular\refimage{planned-transactions-form-img} wird unterhalb der Liste der geplanten Buchungen angezeigt. Es ähnelt dem Formular zur Erfassung gewöhnlicher Buchungen, enthält jedoch im oberen Bereich eine zusätzliche Zeile mit vier bis sechs Feldern zur Angabe:
%The scheduled transactions entry form\refimage{planned-transactions-form-img} appears below the list of scheduled transactions. It is similar to the standard transactions entry form, but includes an additional row at the top with four to six fields for specifying:
%Le formulaire de saisie des opérations planifiées\refimage{planned-transactions-form-img} s'affiche en-dessous de la liste des opérations planifiées. Il est similaire au formulaire de saisie des opérations ordinaires, mais il comprend, dans sa partie supérieure, une ligne supplémentaire de quatre à six champs pour indiquer:

% image centrée
\begin{figure}[htbp]
	\begin{center}
		\includegraphics[width=0.95\textwidth]{image/screenshot/planned_transactions_form}
	\end{center}
	\vspace{-10pt}
	\caption{Geplanter Buchungsformular}
	%\caption{Form for entering scheduled transactions}
	%\caption{Formulaire de saisie des opérations planifiées}
	\label{planned-transactions-form-img}
\end{figure}
% image centrée

\begin{itemize}
	\item des betroffenen Kontos; im Falle einer Umbuchung zwischen zwei Konten können Sie eines der beiden Konten (Absender- oder Empfängerkonto) auswählen; die Art der Buchung (Ausgaben oder Einnahmen) muss im entsprechenden Feld angegeben werden, und die Gegenbuchung wird bei Ausführung der geplanten Buchung automatisch auf dem anderen Konto erstellt;
	%\item the account to be debited; in the case of a transfer between two accounts, you can select either account (source or destination); the transaction type (debit or credit) must be entered in the appropriate field, and the offsetting entry will be automatically created in the other account when the scheduled transaction is executed;
	%\item le compte affecté; dans le cas d'un virement entre deux comptes, vous pouvez sélectionner l'un ou l'autre des deux comptes (origine ou destination); le type de l'opération (débit ou crédit) doit être renseigné dans le champ adéquat, et la contre-opération sera automatiquement créée dans l'autre compte au moment de l'exécution de l'opération planifiée;
	\item die Ausführungsmethode:
	%\item the execution method:
	%\item le mode d'exécution:
	\begin{itemize}
		\item \menus{Manuell}: Sie müssen die Eingabe im Formular manuell ausführen, indem Sie die Funktion \menus{Ausführen} in der Funktionsleiste oder im Kontextmenü auswählen (siehe Abschnitt \vref{plannedtransactions-execution}, \menus{Ausführung einer geplanten Buchung}),
		%\item \menus{Manual}: you will need to manually execute the entry in the form by selecting the \menus{Execute} function from the function bar or the context menu (see the section \vref{plannedtransactions-execution}, \menus{Executing a scheduled transaction}),
		%\item \menus{Manuel}: vous devrez exécuter manuellement la saisie dans le formulaire en sélectionnant la fonction \menus{Exécuter} dans la barre de fonctions ou dans le menu contextuel (voir la section \vref{plannedtransactions-execution}, \menus{Exécution d'une opération planifiée}),
		\item \menus{Automatisch}: die Buchung wird automatisch zum geplanten Datum in die Buchungsliste aufgenommen;
		%\item \menus{Automatic}: the transaction will be automatically entered into the list of transactions on the scheduled date;
		%\item \menus{Automatique}: l'opération sera automatiquement saisie dans la liste des opérations à la date programmée;
	\end{itemize}
	\item the \menus{Frequenz}:
	%\item the \menus{Frequency}:
	%\item la \menus{Périodicité}:
	\begin{itemize}
		\item \menus{Einmal},
		%\item \menus{Once},
		%\item \menus{Une fois},
		\item \menus{Wöchentlich},
		%\item \menus{Weekly},
		%\item \menus{Hebdomadaire},
		\item \menus{Monatlich},
		%\item \menus{Monthly},
		%\item \menus{Mensuelle},
		\item \menus{Zweimonatlich},
		%\item \menus{BimonthlyBimonthly},
		%\item \menus{Bimestrielle},
		\item \menus{Vierteljährlich},
		%\item \menus{Quarterly},
		%\item \menus{Trimestrielle},
		\item \menus{Jährlich},
		%\item \menus{Annually},
		%\item \menus{Annuelle},
		\item \menus{Benutzerdefiniert}\refimage{planned-transactions-form-frequency-custom-img}; In diesem Fall müssen Sie die Anzahl der Zeiträume zwischen jeder Eingabe oder jeder Aufforderung zur Bestätigung des Buchung sowie den gewünschten Zeitraum (Tage, Wochen, Monate, Jahre) genauer angeben;
		%\item \menus{Custom}\refimage{planned-transactions-form-frequency-custom-img}; in this case, you will need to specify in greater detail the number of periods between each entry or request for validation of the transaction, as well as the desired time period (days, weeks, months, years);
		%\item \menus{Personnalisée}\refimage{planned-transactions-form-frequency-custom-img}; dans ce cas, vous devrez préciser plus finement le nombre de périodes entre chaque saisie ou demande de validation de l'opération, ainsi que la période voulue (jours, semaines, mois, années);
		% image centrée
		%\hspace{20pt}
		\item[]	\begin{figure}[htbp]
			\begin{flushright}						% right align
				\captionsetup{%							% for a change of options of the caption package
					justification=centering}			% centring of text and caption label
				\includegraphics[width=0.95\linewidth]{image/screenshot/planned_transactions_form_frequency_custom}
			\end{flushright}
			\caption{Eingabe einer benutzerdefinierten Frequenz}
			%\caption{Entering a custom frequency}
			%\caption{Saisie d'une périodicité personnalisée}
			\label{planned-transactions-form-frequency-custom-img}
		\end{figure}
		% image centrée
	\end{itemize}
	\item die \menus{Fälligkeitstermin}; Die Standardeinstellung ist leer; ein Doppelklick oder die Tastenkombination \keys{Strg+Enter \return} in diesem Feld öffnet einen Kalender; Sie können das Datum auch manuell eingeben, indem Sie die verschiedenen Tastenkombinationen verwenden, wie sie im Abschnitt \vref{transactions-new-dates}, \menus{Eingabe des Datums überdie Tastatur} beschrieben sind.
	%\item the \menus{Limite date} for due dates; the default selection is empty; double-clicking or pressing the key combination \keys{Ctrl+Enter \return} in this field opens a calendar; you can also enter the date manually using the various key combinations described in the section \vref{transactions-new-dates}, \menus{Entering the date using the keyboard}.
	%\item la \menus{Date limite} des échéances; le choix par défaut est vide; un double-clic ou la combinaison de touches \keys{Ctrl+Entrée \return} dans ce champ ouvre un calendrier; vous pouvez aussi saisir manuellement la date en utilisant les multiples combinaisons de touches telles que décrites dans la section \vref{transactions-new-dates}, \menus{Saisie de la date au clavier}.
	\end{itemize}
	
	% espace pour changement de thème
	\vspacepdf{2mm}
	Der untere Teil des Buchungsformular ist identisch mit dem Buchungsformular zur Eingabe gewöhnlicher Buchungen und wird auf dieselbe Weise verwaltet\refimage{planned-transactions-form-img}, siehe Abschnitt \vref{transactions-form}, \menus{Buchungsformular}.
	%The lower part of the form is exactly the same as the form for entering ordinary transactions, and is handled in the same way\refimage{planned-transactions-form-img}, see the section \vref{transactions-form}, \menus{Transaction/Scheduled entry form}.
	%La partie inférieure du formulaire est strictement identique au formulaire de saisie des opérations ordinaires, et est géré de la même manière\refimage{planned-transactions-form-img}, voir la section \vref{transactions-form}, \menus{Formulaire de saisie des opérations}.

	% espace pour changement de thème
	\vspacepdf{2mm}
	Sobald das Buchungsformular angezeigt wird, ermöglicht ein Kontextmenü, das durch einen \cmds{Rechtsklick} in ein Eingabefeld aufgerufen wird, die folgenden Aktionen:
	%Once the form is displayed, a context menu accessible by \cmds{right-clicking} in an input field allows you to perform the following actions:
	%Une fois le formulaire affiché, un menu contextuel accessible par un \cmds{clic-droit} dans un champ de saisie permet d'effectuer les actions suivantes:
	
	\begin{itemize}
	\item \menus{Ausschneiden} (der Auswahl);
	%\item \menus{Cut} (the selection);
	%\item \menus{Couper} (la sélection);
	\item \menus{Kopieren} (der Auswahl);
	%\item \menus{Copy} (the selection);
	%\item \menus{Copier} (la sélection);
	\item \menus{Einfügen} (der Auswahl);
	%\item \menus{Paste} (the selection);
	%\item \menus{Coller} (la sélection);
	\item \menus{Löschen} (der Auswahl);
	%\item \menus{Delete} (the selection);
	%\item \menus{Supprimer} (la sélection);
	\item[]\hspace{-12pt} dann
	%\item[]\hspace{-12pt} then
	%\item[]\hspace{-12pt} puis
	\item \menus{Alles auswählen} (im Eingabefeld);
	%\item \menus{Select All} (in the input field);
	%\item \menus{Tout sélectionner} (dans le champ de saisie);
	\item \menus{Emoji einfügen}\index{émoji}.
	%\item \menus{Insert Emoji}\index{émoji}.
	%\item \menus{Insérer un émoji}\index{émoji}.
	\end{itemize}
	
	% espace pour changement de thème
	\vspacepdf{2mm}
	Darüber hinaus ermöglicht ein weiteres Kontextmenü, das durch einen Rechtsklick im Buchungsformular in einem grauen Bereich außerhalb der Eingabefelder aufgerufen wird, den Zugriff auf die Formularkonfiguration durch Auswahl von \menus{Buchungsformular Einstellungen} (siehe Abschnitt \vref{setup-form}, \menus{Buchungsformular}).
	%In addition, another context menu, accessible by right-clicking in the entry form within a grey area outside the input fields, allows you to access the form’s configuration by selecting \menus{Configure the form} (see the section \vref{setup-form}, \menus{Transactions form}).
	%De plus, un autre menu contextuel accessible par un clic-droit dans le formulaire de saisie, dans une zone grise en dehors des champs de saisie, permet d'accéder à la configuration du formulaire, en sélectionnant \menus{Configurer le formulaire} (voir la section \vref{setup-form}, \menus{Formulaire des opérations}).
	
	% espace pour changement de thème
	\vspacepdf{2mm}
	Bitte beachten Sie, dass die Auswahl der Buchungstypen zwangsläufig mit dem ausgewählten Kontotyp verknüpft ist (siehe Abschnitt \vref{accounts-type}, \menus{Arten von Grisbi-Konten}).
	%Please note that the choice of transaction types is necessarily linked to the selected account type (see the section \vref{accounts-type}, \menus{Types of Grisbi accounts}).
	%Notez que le choix des types d'opérations est nécessairement lié au type du compte sélectionné (voir la section \vref{accounts-type}, \menus{Types de compte de Grisbi}).


\section{Kalender und Prognosen\label{plannedtransactions-calendar}}
%\section{Calendar and forecasts\label{plannedtransactions-calendar}}
%\section{Calendrier et prévisions\label{plannedtransactions-calendar}}

Am unteren Rand des Navigationsbereichs\refimage{planned-transactions-display-img} wird ein Kalender\refimage{planned-transactions-calendar-img} angezeigt. Der aktuelle Tag ist \colorbox[RGB]{198,200,201}{hervorgehoben}, und die \textbf{fettgedruckten} Daten entsprechen den Terminen der für diesen Tag geplanten Buchungen für alle Konten.
%A calendar\refimage{planned-transactions-calendar-img} is displayed at the bottom of the navigation panel\refimage{planned-transactions-display-img}. The current day is \colorbox[RGB]{198,200,201}{highlighted}, and the dates \textbf{in bold} correspond to the dates of scheduled transactions for all accounts due on that day.
%Un calendrier\refimage{planned-transactions-calendar-img} s'affiche en bas du panneau de navigation\refimage{planned-transactions-display-img}. Le jour en cours est \colorbox[RGB]{198,200,201}{surligné}, et les dates \textbf{en gras} correspondent aux dates des opérations planifiées de tous les comptes arrivant à échéance.


\vspace{5mm}
\noindent
\begin{minipage}{.6\linewidth}
	\vspace{-15pt}
	\begin{itemize}[rightmargin=.6cm]
		\item Mit den Tasten \keys{\arrowkeyleft} oder \keys{\arrowkeyright} können Sie zum vorherigen oder nächsten Tag wechseln;
		%\item the \keys{\arrowkeyleft} or \keys{\arrowkeyright} keys allow you to move to the previous or next day;
		%\item les touches \keys{\arrowkeyleft} ou \keys{\arrowkeyright}, permettent de passer au jour précédent ou suivant;
		\item mit den Tasten \keys{\arrowkeyup} oder \keys{\arrowkeydown} können Sie zur vorherigen oder nächsten Woche wechseln;
		%\item the \keys{\arrowkeyup} or \keys{\arrowkeydown} keys allow you to move to the previous or next week;
		%\item les touches \keys{\arrowkeyup} ou \keys{\arrowkeydown} permettent de passer à la semaine précédente ou suivante;
		\item Mit den Tastenkombinationen \keys{Strg+\arrowkeyleft} oder \keys{Strg+\arrowkeyright} können Sie zum vorherigen oder nächsten Monat wechseln;
		%\item the key combination \keys{Ctrl+\arrowkeyleft} or \keys{Ctrl+\arrowkeyright} allows you to move to the previous or next month;
		%\item la combinaison de touches \keys{Ctrl+\arrowkeyleft} ou \keys{Ctrl+\arrowkeyright} permet de passer au mois précédent ou suivant;
		\item mit den Tastenkombinationen \keys{Strg+\arrowkeyup} oder \keys{Strg+\arrowkeydown} können Sie zum vorherigen oder nächsten Jahr wechseln.
		%\item the \keys{Ctrl+\arrowkeyup} or \keys{Ctrl+\arrowkeydown} key combinations allow you to move to the previous or next year.
		%\item la combinaison de touches \keys{Ctrl+\arrowkeyup} ou \keys{Ctrl+\arrowkeydown} permet de passer à l'année précédente ou suivante.
	\end{itemize}
\end{minipage}
\hspace{10pt}
\begin{minipage}{.4\linewidth}
	%\begin{wrapfigure}{R}{0.4\textwidth}%50mm
	%\vspace{-\intextsep}				% space above the floating (minus intextsep=separation between float and text in text)
	\vspace{-10pt}
	%\centering							% centering the floating figure in the "wrap"
	\raggedleft
	\includegraphics[width=.9\textwidth]{image/screenshot/planned_transactions_calendar}
	%\vspace{-15pt}						% space between the floating and the caption below the floating
	\captionsetup{%						% for a change of options of the caption package
		format=plain,					% to avoid error message with labelsep option below, default=hang
		type=figure,%
		name=Fig.,%						% rename label caption, default="Figure" (or "Table")
		justification=centering,		% centring of text and caption label 
		labelsep=newline}				% define the separator between the label and the caption text
	\caption{Fälligkeitskalender}		% \caption is mandatory to reference a figure in lof
	%\caption{Due dates calendar}		% \caption is mandatory to reference a figure in lof
	%\caption{Calendrier des échéances}		% \caption is mandatory to reference a figure in lof
	%\vspace{-10pt}						% space below the caption
	\label{planned-transactions-calendar-img}
	%\end{wrapfigure}
\end{minipage}


\section{Auswahl einer geplanten Buchung\label{plannedtransactions-selection}}
%\section{Selecting a scheduled transaction\label{plannedtransactions-selection}}
%\section{Sélection d'une opération planifiée\label{plannedtransactions-selection}}

Es gibt zwei Möglichkeiten, eine geplante Buchung auszuwählen:
%There are two ways to select a scheduled transaction:
%Pour sélectionner une opération planifiée, vous avez deux moyens:
\begin{itemize}
	\item Klicken Sie auf die entsprechende Zeile;
	%\item click on its row;
	%\item cliquer sur sa ligne;
	\item bewegen Sie die Auswahl mit den Tasten \keys{\arrowkeyup} und \keys{\arrowkeydown} auf der Tastatur.
	%\item move the selection using the \keys{\arrowkeyup} and \keys{\arrowkeydown} keys on the keyboard.
	%\item déplacer la sélection avec les touches \keys{\arrowkeyup} et \keys{\arrowkeydown} du clavier.
\end{itemize}

Der Buchung wird dann auf \colorbox[RGB]{245,155,155}{dunkelrosa Hintergrund} angezeigt und seine Details werden im Buchungsformular angezeigt.
%The transaction will then appear on a \colorbox[RGB]{245,155,155}{dark pink background} and its details will be displayed in the form.
%L'opération apparaît alors sur \colorbox[RGB]{245,155,155}{fond rose foncé} et ses détails s'affichent dans le formulaire.

\vspace{2mm}
Ein \cmds{Rechtsklick} auf die Zeile eines geplanten Buchung öffnet ein Kontextmenü mit den folgenden Funktionen\refimage{planned-transactions-contextMenu-img}:
%A \cmds{right-click} on the row of a scheduled transaction displays a context menu with the following functions\refimage{planned-transactions-contextMenu-img}:
%Un \cmds{clic-droit} sur la ligne d'une opération planifiée affiche un menu contextuel avec les fonctions suivantes\refimage{planned-transactions-contextMenu-img}:

\vspace{3mm}
\noindent
\begin{minipage}{.65\linewidth}
	\vspace{-25pt}
	\begin{itemize}[rightmargin=.6cm]
		\item \menus{Buchung bearbeiten};
		%\item \menus{Edit transaction};
		%\item \menus{Éditer l'opération};
		\item \menus{Buchung duplizieren};
		%\item \menus{Clone transaction};
		%\item \menus{Cloner l'opération};
		\item \menus{Buchung erstellen};
		%\item \menus{New transaction};
		%\item \menus{Nouvelle opération};
		\item \menus{Buchung löschen};
		%\item \menus{Delete transaction};
		%\item \menus{Supprimer l'opération};
		\item \menus{Notizen anzeigen} oder \menus{Frequenz/Art anzeigen};
		%\item \menus{Displays notes} or \menus{Displays Frequency/Mode};
		%\item \menus{Afficher les remarques} ou \menus{Afficher Périodicité/Mode};
		\item \menus{Buchung ausführen}.
		%\item \menus{Execute transaction}.
		%\item \menus{Exécuter l'opération}.
	\end{itemize}
\end{minipage}
\hspace{10pt}
\begin{minipage}{.35\linewidth}
	%\begin{wrapfigure}{R}{0.4\textwidth}%50mm
	%\vspace{-\intextsep}				% space above the floating (minus intextsep=separation between float and text in text)
	%\vspace{-20pt}
	\centering							% centering the floating figure in the "wrap"
	%\raggedleft
	\includegraphics[width=.9\textwidth]{image/screenshot/planned_transactions_contextMenu}
	%\vspace{-15pt}						% space between the floating and the caption below the floating
	\captionsetup{%						% for a change of options of the caption package
		format=plain,					% to avoid error message with labelsep option below, default=hang
		type=figure,%
		name=Fig.,%						% rename label caption, default="Figure" (or "Table")
		justification=centering,		% centring of text and caption label 
		labelsep=newline}				% define the separator between the label and the caption text
	\caption{Kontextmenü für\\ eine geplante Buchung}		% \caption is mandatory to reference a figure in lof
	%\caption{Context menu\\ for a scheduled transaction}		% \caption is mandatory to reference a figure in lof
	%\caption{Menu contextuel\\ d'une opération planifiée}		% \caption is mandatory to reference a figure in lof
	%\vspace{-10pt}						% space below the caption
	\label{planned-transactions-contextMenu-img}
	%\end{wrapfigure}
\end{minipage}


\section{Neuer geplanter Buchung\label{plannedtransactions-new}}
%\section{New scheduled transaction\label{plannedtransactions-new}}
%\section{Nouvelle opération planifiée\label{plannedtransactions-new}}

Die Eingabe eines geplanten Buchung erfolgt ähnlich wie die Eingabe eines Standardbuchung.
%Entering a scheduled transaction is done in a similar way to entering a standard transaction.
%La saisie d'une opération planifiée se fait d'une manière similaire à la saisie d'une opération ordinaire.

Um das Buchungsformular \indexword{zu öffnen oder anzuzeigen}\index{Buchungsformular!anzeigen}, verwenden Sie eine der folgenden Methoden:
%To \indexword{open or display}\index{entry form!display} the entry form, use one of the following methods:
%Pour \indexword{ouvrir ou afficher}\index{formulaire de saisie!afficher} le formulaire de saisie, utilisez une des méthodes suivantes:
\begin{itemize}
	\item Wählen Sie in der Menüleiste \menus{Bearbeiten - Buchung erstellen};
	%\item from the menu bar, select \menus{Edit - New transaction};
	%\item dans la barre de menu, sélectionnez \menus{Éditer - Nouvelle opération};
	\item Wählen Sie in der Menüleiste \menus{Ansicht} und aktivieren Sie das Kontrollkästchen für die Option \menus{Buchungsformular anzeigen};
	%\item from the menu bar, select \menus{View} and tick the box for the \menus{Show transaction form} option;
	%\item dans la barre de menu, sélectionnez \menus{Affichage} et cochez la case de l'option \menus{Montrer le formulaire de saisie des opérations};
	\item Markieren Sie die leere Zeile am Ende der Buchungsliste und drücken Sie die Taste \keys{Enter \return};
	%\item select the empty line at the bottom of the list of transactions and press the \keys{Enter \return} key;
	%\item sélectionnez la ligne vide en bas de la liste des opérations et appuyez sur la touche \keys{Entrée \return};
	\item Klicken Sie auf die leere Zeile am Ende der Buchungsliste;
	%\item click on the empty line at the bottom of the list of transactions;
	%\item cliquez sur la ligne vide en bas de la liste des opérations;
	\item Drücken Sie die Tastenkombination \keys{Strg+T};
	%\item press the \keys{Ctrl+T} key combination;
	%\item appuyez sur la combinaison de touches \keys{Ctrl+T};
	\item Klicken Sie in der Symbolleiste auf \menus{Geplante Buchung erstellen};
	%\item in the toolbar, click \menus{New scheduled};
	%\item dans la barre d'outils, cliquez sur \menus{Nouvelle échéance};
	\item Wählen Sie im Kontextmenü der Buchungsliste \menus{Buchung erstellen} aus;
	%\item in the context menu of the transaction list, select \menus{New Transaction};
	%\item dans le menu contextuel de la liste des opérations, sélectionnez \menus{Nouvelle opération};
	\item Klicken Sie auf das kleine Dreieck neben der Beschriftung \indexword{anzeigen/ausblenden}\index{Buchungsform!anzeige/ausblenden} im Formular für Buchungsformular links neben der Beschriftung.
	%\item click on the small \indexword{show/hide}\index{entry form!hide-show} triangle on the transaction form to the left of its label.
	%\item cliquez sur le petit triangle d'\indexword{affichage/masquage}\index{formulaire de saisie!masquer-afficher} du formulaire de saisie des opérations à gauche de son libellé.
	
	% saut de ligne pour indentation correcte de la note dans la liste
	\vspacepdf{2mm}
	\Note{}: Je nach Design deiner Desktop-Umgebung oder des von dir verwendeten Fenstermanagers können diese Dreiecke durch andere Zeichen wie +, -, >, < usw. ersetzt werden.
	%\Note{}: depending on the theme of your desktop environment or the window manager you are using, this triangle may be replaced by other characters such as +, -, >, <, etc.
	%\Note{}: ce triangle peut être remplacé, en fonction du thème de l'environnement de bureau ou du gestionnaire de fenêtres que vous utilisez, par d'autres caractères tels que +, -, >, <, etc.
\end{itemize}

% espace pour changement de thème
\vspacepdf{2mm}
Füllen Sie die Felder mit den Details Ihrer geplanten Transaktion aus. Weitere Informationen zur Eingabe dieser Details in das Formular finden Sie in den Abschnitten \vref{plannedtransactions-form}, \menus{Geplanter Buchungsformular} und \vref{plannedtransactions-calendar}, \menus{Kalender und Prognosen}.
%Fill in the fields with the details of your scheduled transaction. For more details on entering these details into the form, see the sections \vref{plannedtransactions-form}, \menus{Transaction/Scheduled form} and \vref{plannedtransactions-calendar}, \menus{Calendar and Forecasts}.
%Renseignez les champs avec les paramètres de votre opération planifiée. Pour plus de détails sur la saisie de ces paramètres dans le formulaire, voir les sections \vref{plannedtransactions-form}, \menus{Formulaire de saisie des opérations planifiées} et \vref{plannedtransactions-calendar}, \menus{Calendrier et prévisions}.


\section{Splitt einer geplanten Buchung\label{plannedtransactions-breakdown}}
%\section{Breakdown of a scheduled transaction\label{plannedtransactions-breakdown}}
%\section{Ventilation d'une opération planifiée\label{plannedtransactions-breakdown}}

Die Splitt einer geplanten Buchung erfolgt ähnlich wie bei einer gewöhnlichen Buchung. Nachdem Sie die Felder in der ersten Zeile des Formulars ausgefüllt haben (siehe Abschnitt \vref{plannedtransactions-form}, \menus{Geplanter Buchungsformular}), geben Sie die übrigen Buchungsparameter im unteren Teil dieses Buchungsformulars ein (siehe Abschnitt \vref{transactions-form}, \menus{Buchungsformular}) ein und fahren Sie mit der Eingabe der aufgeteilten geplanten Buchung fort, wie im Abschnitt \vref{transactions-breakdown}, \menus{Splittbuchung} ausführlich beschrieben.
%The breakdown of a scheduled transaction is carried out in a similar way to that of an ordinary transaction. Once you have completed the fields on the first line of the form (see section \vref{plannedtransactions-form}, \menus{Transaction/Scheduled form}), enter the other transaction parameters in the lower part of this form (see section \vref{transactions-form}, \menus{Transaction/Scheduled entry form}), and proceed to enter the split scheduled transaction, as described in detail in the section \vref{transactions-breakdown}, \menus{Breakdown of a transaction}.
%La ventilation d'une opération planifiée se fait d'une manière similaire à la ventilation d'une opération ordinaire. Une fois que vous avez renseigné les champs de la première ligne du formulaire (voir la section \vref{plannedtransactions-form}, \menus{Formulaire de saisie des opérations planifiées}), saisissez les autres paramètres de l'opération dans la partie inférieure de ce formulaire (voir la section \vref{transactions-form}, \menus{Formulaire de saisie des opérations}), et procédez à la saisie de l'opération planifiée ventilée, comme décrit précisément dans la section \vref{transactions-breakdown}, \menus{Ventilation d'une opération}.


\section{Eine geplante Buchung bearbeiten\label{plannedtransactions-modify}}
%\section{Edit a scheduled transaction\label{plannedtransactions-modify}}
%\section{Modification d'une opération planifiée\label{plannedtransactions-modify}}

Um eine geplante Buchung, eine geplante Buchung mit Aufschlüsselung oder eine ihrer Teilbuchungen zu bearbeiten, gehen Sie wie folgt vor:
%To edit a scheduled transaction, a scheduled transaction with a breakdown, or one of its sub-transactions, proceed as follows:
%Pour modifier une opération planifiée, ou une opération planifiée ventilée ou une de ses sous-opérations, procédez comme suit:

\begin{enumerate}
	\item Wählen Sie sie entweder aus:
	%\item select it either:
	%\item sélectionnez-la soit:
	\begin{itemize}
		\item sie aus der Liste der Buchungen auswählen, wo sie auf einem \colorbox[RGB]{245,155,155}{dunkelrosa Hintergrund} angezeigt wird,
		%\item by selecting it from the list of transactions where it will be displayed on a \colorbox[RGB]{245,155,155}{dark pink background},
		%\item par sélection dans la liste des opérations où elle s'affichera sur \colorbox[RGB]{245,155,155}{fond rose foncé},
		\item indem Sie mit der \cmds{linken Maustaste} auf das Werkzeug \menus{Bearbeiten} in der Symbolleiste klicken,
		%\item by \cmds{left-clicking} on the \menus{Edit} tool in the toolbar,
		%\item par \cmds{clic-gauche} sur l'outil \menus{Éditer} de la barre d'outils,
		\item indem Sie mit der \cmds{rechten Maustaste} auf die Buchungszeile klicken und dann im Kontextmenü \menus{Buchung bearbeiten} auswählen;
		%\item by \cmds{right-clicking} on the transaction line and then selecting \menus{Edit transaction} from the context menu;
		%\item par \cmds{clic-droit} sur la ligne de l'opération puis sélection de \menus{Éditer l'opération} dans le menu contextuel;
	\end{itemize}
	\item das Buchungsformular wird angezeigt und das Feld \menus{Datum} ist ausgewählt: Sie können nun alle Buchungsdetails nach Belieben bearbeiten;
	%\item the form appears and the \menus{Date} field is selected: you can then edit all the transaction details as you wish;
	%\item le formulaire de saisie s'affiche et le champ \menus{Date} est sélectionné: vous pouvez alors modifier toutes les données de l'opération à votre convenance;
	\item bestätigen Sie durch Klicken auf die Schaltfläche \menus{OK}.
	%\item confirm by clicking the \menus{Validate} button.
	%\item validez par le bouton \menus{Valider}.
\end{enumerate}


\section{Duplizieren einer geplanten Buchung\label{plannedtransactions-duplicate}}
%\section{Cloning a scheduled transaction\label{plannedtransactions-duplicate}}
%\section{Clonage d'une opération planifiée\label{plannedtransactions-duplicate}}

Um einen geplanten Buchung zu duplizieren, wählen Sie ihn aus, sodass er im Buchungsformular angezeigt wird, und verwenden Sie dann eine der beiden folgenden Methoden:
%To clone a scheduled transaction, select it so that it appears in the form, then use one of the following two methods:
%Pour cloner une opération planifiée, sélectionnez-la pour qu'elle s'affiche dans le formulaire de saisie, puis utilisez l'une de ces deux méthodes:
\begin{itemize}
	\item Wählen Sie in der Menüleiste \menus{Bearbeiten - Buchung duplizieren};
	%\item in the menu bar, select \menus{Edit - Clone transaction};
	%\item dans la barre de menus, sélectionnez \menus{Éditer - Cloner l'opération};
	\item Wählen Sie im Kontextmenü \menus{Buchung duplizieren}.
	%\item from the context menu, select \menus{Clone transaction}. 
	%\item dans le menu contextuel, sélectionnez \menus{Cloner l'opération}.
\end{itemize}

% espace pour changement de thème
\vspacepdf{2mm}
Der zuvor ausgewählte Buchung wird weiterhin auf \colorbox[RGB]{245,155,155}{dunkelrosa Hintergrund} angezeigt, und der geklonte Buchung wird in einer neuen Zeile darunter angezeigt.
%The selected transaction is still displayed on a \colorbox[RGB]{245,155,155}{dark pink background}, and the cloned transaction appears on a new line below it.
%L'opération sélectionnée précédemment apparaît toujours sur \colorbox[RGB]{245,155,155}{fond rose foncé}, et l'opération clonée s'affiche dans une nouvelle ligne en-dessous.

Sie können diesen neuen Buchung nun bearbeiten, um beispielsweise das Datum zu ändern.
%You can then edit this new transaction, for example, to change the date.
%Vous pouvez alors éditer cette nouvelle opération pour, par exemple, en modifier la date.


\section{Notizen zu geplanten Buchungen\label{plannedtransactions-comments}}
%\section{Notes on scheduled transactions\label{plannedtransactions-comments}}
%\section{Remarques des opérations planifiées\label{plannedtransactions-comments}}

Sie können die Notizen auf zwei Arten ein- oder ausblenden:
%You can show or hide notes in two ways:
%Vous pouvez afficher ou cacher les remarques de deux manières:
\begin{itemize}
	\item Klicken Sie in der Symbolleiste auf das Symbol \menus{Notizen};
	%\item click on the \menus{Notes} tool in the toolbar;
	%\item cliquez sur l'outil \menus{Remarques} dans la barre d'outils;
	\item klicken Sie mit der rechten Maustaste auf die Zeile der Buchung und wählen Sie im Kontextmenü \menus{Notizen anzeigen} aus.
	%\item right-click on the transaction row and select \menus{Displays notes} from the context menu.
	%\item cliquez-droit sur la ligne de l'opération et sélectionnez \menus{Afficher les remarques} dans le menu contextuel.
\end{itemize}

Die Notizen zu allen Buchungen werden dann anstelle der Spalten \menus{Frequenz} und \menus{Art} in der Liste der geplanten Buchungen\refimage{planned-transactions-list-comments-img} angezeigt. Wenn Sie denselben Vorgang wiederholen, werden die Spalten \menus{Frequenz} und \menus{Modus} anstelle der Spalte \menus{Notizen} wiederhergestellt.
%The notes for all transactions are then displayed in place of the \menus{Frequency} and \menus{Mode} columns in the list of scheduled transactions\refimage{planned-transactions-list-comments-img}. Repeating the same sequence will restore the \menus{Frequency} and \menus{Mode} columns in place of the \menus{Notes} column.
%Les remarques de toutes les opérations s'affichent alors à la place des colonnes \menus{Périodicité} et \menus{Mode} dans la liste des opérations planifiées\refimage{planned-transactions-list-comments-img}. Recommencer la même séquence réaffiche les colonnes \menus{Périodicité} et \menus{Mode} à la place de la colonne \menus{Remarques}.

% image centrée
\begin{figure}[htbp]
	\begin{center}
		\includegraphics[width=0.95\textwidth]{image/screenshot/planned_transactions_list_comments}
	\end{center}
	\caption{Anzeigen von Notizen zu geplanten Buchungen}
	%\caption{Displaying notes for scheduled transactions}
	%\caption{Affichage des remarques des opérations planifiées}
	\label{planned-transactions-list-comments-img}
\end{figure}


\section{Ausführen einer geplanten Buchung\label{plannedtransactions-execution}}
%\section{Executing a scheduled transaction\label{plannedtransactions-execution}}
%\section{Exécution d'une opération planifiée\label{plannedtransactions-execution}}

Mit dieser Funktion können Sie die Ausführung einer geplanten Buchung manuell außerhalb des geplanten Termins erzwingen.
%This function allows you to manually force the execution of a scheduled transaction outside the scheduled date.
%Cette fonction permet de forcer manuellement l'exécution d'une opération planifiée en dehors de la date prévue.

Die Funktion \menus{Ausführen} in der Symbolleiste dient ausschließlich dazu, die Details einer aus der Liste der geplanten Buchungen ausgewählten Buchung in das Buchungsformular zu kopieren. Damit diese Buchung tatsächlich in die Buchungsliste des entsprechenden Kontos übernommen wird, müssen Sie das Buchungsformular speichern, nachdem Sie gegebenenfalls Änderungen an der Buchung vorgenommen haben.
%The \menus{Execute} function on the toolbar is used solely to copy the details of a transaction selected from the list of scheduled transactions into the transaction/scheduled form. For this instance to be effectively transferred to the list of transactions for the relevant account, you must then save the Transaction/Scheduled form, after making any necessary changes to the transaction.
%La fonction \menus{Exécuter} de la barre d'outils sert uniquement à faire la recopie, dans le formulaire de saisie, de l'occurrence d'une opération sélectionnée dans la liste des opérations planifiées. Pour que cette occurrence soit effectivement transférée dans la liste des opérations du compte concerné, il est nécessaire de valider ensuite le formulaire de saisie, après avoir éventuellement modifié l'opération.

% espace pour changement de thème
\vspacepdf{2mm}
Um eine geplante Buchung auszuführen, gehen Sie wie folgt vor:
%To execute an instance of a scheduled transaction, proceed as follows:
%Pour exécuter une occurrence d'une opération planifiée, procédez comme suit:
\begin{enumerate}
	\item Wählen Sie sie aus der Liste aus;
	%\item select it from the list;
	%\item sélectionnez-la dans la liste;
	\item führen Sie sie dann aus, indem Sie:
	%\item then execute it by:
	%\item exécutez-la ensuite avec:
	\begin{itemize}
		\item entweder mit der \cmds{linken Maustaste} auf das Werkzeug \menus{Ausführen} in der Symbolleiste klicken,
		%\item either \cmds{left-clicking} on the \menus{Execute} tool in the toolbar,
		%\item soit un \cmds{clic-gauche} sur l'outil \menus{Exécuter} de la barre d'outils,
		\item oder mit der \cmds{rechten Maustaste} auf die Buchungszeile klicken und im Kontextmenü \menus{Buchung ausführen} auswählen;
		%\item or \cmds{right-clicking} on the transaction line and selecting \menus{Execute} from the context menu;
		%\item soit un \cmds{clic-droit} sur la ligne de l'opération puis la sélection d' \menus{Exécuter l'opération} dans le menu contextuel;
	\end{itemize}
	\item Die ausgewählte Buchung wird dann im Buchungsformular angezeigt, wobei die oberste Zeile der Parameter der geplanten Buchung ausgeblendet ist; Sie können sie anschließend nach Bedarf bearbeiten;
	%\item the selected transaction is then displayed in the transaction/scheduled form, with the top row of the scheduled transaction’s parameters hidden; you can then edit it as required;
	%\item l'occurrence sélectionnée alors s'affiche dans le formulaire de saisie avec le masquage de la ligne supérieure des paramètres de l'opération planifiée; vous pouvez ensuite la modifier selon votre besoin;
	\item Bestätigen Sie durch Klicken auf die Schaltfläche \menus{OK}; die Instanz der Buchung wird in die Buchungsliste des entsprechenden Kontos übernommen, und in der Liste der geplanten Buchungen wird die nächste Instanz (sofern vorhanden, da möglicherweise nur eine geplante Buchung vorliegt) der geplanten Buchung angezeigt.
	%\item confirm by clicking the \menus{Validate} button; the instance of the transaction is transferred to the list of transactions for the relevant account, and the list of scheduled transactions displays the next instance (if any, as there may be only one scheduled transaction) of the scheduled transaction.
	%\item validez avec le bouton \menus{Valider}; l'occurrence de l'opération est transférée dans la liste des opérations du compte concerné, et la liste des opérations planifiées affiche l'occurrence suivante (si elle existe, car l'opération planifiée peut être unique) de l'opération planifiée.
\end{enumerate}

% espace avant Attention ou Note : 5 mm
\vspacepdf{2mm}
\Note{}: Nach der Bestätigung wird die Überweisung ohne weitere Benachrichtigung ausgeführt.
%\Note{}: Once confirmed, the transfer is carried out without further notification.
%\Note{}: après cette validation, le transfert est fait sans autre avertissement.

% espace pour changement de thème
\vspacepdf{2mm}
Sie können außerdem überprüfen, ob die geplante Buchung nach ihrer Bestätigung in der Liste der Buchungen des entsprechenden Kontos erscheint.
%You can also check that, once the scheduled transaction has been validated, it appears in the list of transactions for the relevant account.
%Vous pouvez aussi vérifier que la validation de l'exécution de l'occurrence de l'opération planifiée affiche bien ladite opération dans la liste des opérations du compte concerné.


\section{Geplanten Buchung löschen\label{plannedtransactions-remove}}
%\section{Deleting a scheduled transaction\label{plannedtransactions-remove}}
%\section{Suppression d'une opération planifiée\label{plannedtransactions-remove}}

Um eine geplante Buchung zu löschen, gehen Sie wie folgt vor:
%To delete a scheduled transaction, proceed as follows:
%Pour supprimer une opération planifiée, procédez comme suit:
\begin{enumerate}
	\item Wählen Sie sie aus der Liste aus;
	%\item select it from the list;
	%\item sélectionnez-la dans la liste;
	\item dann entweder:
	%\item then either:
	%\item puis après soit:
	\begin{itemize}
		\item Klicken Sie mit der linken Maustaste auf das Werkzeug \menus{Löschen} in der Symbolleiste,
		%\item left-click on the \menus{Delete} tool in the toolbar,
		%\item un \cmds{clic-gauche} sur l'outil \menus{Supprimer} dans la barre d'outils,
		\item oder klicken Sie mit der rechten Maustaste auf die Zeile der geplanten Buchung und wählen Sie \menus{Buchung löschen} aus dem Kontextmenü;
		%\item or right-click on the line for the scheduled task and select \menus{Delete transaction} from the context menu;
		%\item ou un \cmds{clic-droit} sur la ligne de l'opération planifiée puis la sélection de \menus{Supprimer l'opération} dans le menu contextuel;
	\end{itemize}
	\item Es öffnet sich ein Dialogfeld\refimage{planned-transactions-delete-img}, in dem Sie zum Löschen aufgefordert werden:
	%\item a dialogue box opens\refimage{planned-transactions-delete-img} and asks you to delete:
	%\item une boîte de dialogue s'ouvre\refimage{planned-transactions-delete-img} et vous propose de supprimer:
	\begin{figure}[htbp]
		\begin{center}
			\includegraphics[width=0.5\textwidth]{image/screenshot/planned_transactions_delete}
		\end{center}
		\vspace{-10pt}
		\caption{Löschen einer geplanten Buchung}
		%\caption{Deleting a scheduled transaction}
		%\caption{Suppression d'une opération planifiée}
		\vspace{-10pt}
		\label{planned-transactions-delete-img}
	\end{figure}
	\begin{itemize}
		\item \menus{Gesamte buchung}: Alle Buchungen, sowohl aktuelle als auch zukünftige, werden gelöscht; vergangene Buchungen bleiben erhalten,
		%\item \menus{All the occurencies}: all transactions, both current and future, will be deleted; past transactions are retained,
		%\item \menus{Toutes les occurrences}: toutes les opérations, courante et futures, seront supprimées, les opérations passées sont conservées, 
		\item \menus{Nur diese Buchung}: Das heißt, nur die aktuelle Instanz (nur die ausgewählte Buchung wird gelöscht);
		%\item \menus{Only this one}: that is, only the current occurence (only the selected transaction will be deleted);
		%\item \menus{Juste celle-çi}: c'est-à-dire uniquement l'occurrence courante (seule l'opération sélectionnée sera supprimée);
		\item Über das Kontrollkästchen \menus{Auswahl beibehalten und mit Meldung nicht mehr anzeigen?} können Sie Ihre Auswahl für zukünftige Löschvorgänge speichern, sodass das Dialogfeld nicht mehr erscheint;
		%\item the tick box \menus{Keep this choice and do longer see this message?} allows you to save your choice for future deletions, and the dialogue box will no longer appear;
		%\item la case à cocher \menus{Gardez ce choix et ne plus voir ce message?} permet de conserver votre choix pour les suppressions futures et la boîte de dialogue ne s'affichera plus;
		
		\vspacepdf{2mm}
		\Attention{}: wenn Sie dieses Kontrollkästchen aktivieren, erfolgen zukünftige Löschvorgänge sofort, ohne Warnung und ohne Dialogfeld.
		%\Attention{}: by ticking this box, future deletions will take place immediately, without warning and without a dialogue box.
		%\Attention{}: en cochant cette case, les suppression futures seront immédiates, sans avertissement et sans boîte de dialogue.
		
		\vspacepdf{2mm}
		\Note{}: Sie können die Anzeige des Löschdialogfelds in den Grisbi-Einstellungen wieder aktivieren; siehe das Kapitel \vref{setup-operations-remove} \menus{Löschrückfragen}.
		%\Note{}: you can re-enable the display of the deletion dialogue box in Grisbi’s preferences; see the chapter \vref{setup-operations-remove} \menus{Messages before deleting}.
		%\Note{}: vous pourrez rétablir l'affichage de la boîte de dialogue de suppression dans les préférence de Grisbi, voir le chapitre \vref{setup-operations-remove} \menus{Messages avant suppression}.
	\end{itemize}
	\item Treffen Sie Ihre Wahl; der Löschvorgang wird sofort ausgeführt.
	%\item make your choice; the deletion is carried out immediately.
	%\item faites votre choix, la suppression est exécutée immédiatement.
\end{enumerate}

% espace avant Attention ou Note : 5 mm
\vspacepdf{2mm}
\Attention{}: Unmittelbar nach der Auswahl von entweder \menus{Gesamte buchung} oder \menus{Nur diese Buchung} wird der Löschvorgang ohne weitere Warnung ausgeführt.
%\Attention{}: immediately after selecting either \menus{All the occurrencies} or \menus{Only this one}, the deletion is carried out without further warning.
%\Attention{}: immédiatement après le choix entre \menus{Toutes les occurrences} et \menus{Juste celle-ci}, la suppression est exécutée sans autre avertissement.

% espace avant Attention ou Note : 5 mm
\vspacepdf{2mm}
\Attention{}: Das Löschen eines geplanten Buchung ist \indexword{unwiderruflich};\index{Buchung!unwiderruflich}!
%\Attention{}: deleting a scheduled transaction is \indexword{irreversible};\index{transaction!irreversible}!
%\Attention{}: la suppression d'une opération planifiée est \indexword{irréversible}\index{opération!irréversible}!
